# Horizon Configuration

broker:
  id: 1
  host: "0.0.0.0"
  port: 9092
  cluster_id: "horizon-cluster"

storage:
  backend: "file"                # "file" | "s3" | "redis" | "infinispan"
  data_dir: "./data"             # used by file backend
  segment_size_mb: 1024          # 1GB per segment
  retention_hours: 168           # 7 days retention
  sync_writes: false             # Sync writes to disk
  flush_interval: 1s             # Background flush interval

  # --- S3 / MinIO backend ---
  # s3:
  #   bucket: "horizon-data"
  #   prefix: ""
  #   region: "us-east-1"
  #   endpoint: ""               # override for MinIO, e.g. "http://minio:9000"
  #   access_key: ""
  #   secret_key: ""

  # --- Redis backend ---
  # redis:
  #   addr: "localhost:6379"
  #   password: ""
  #   db: 0
  #   key_prefix: "horizon"

  # --- Infinispan backend ---
  # infinispan:
  #   url: "http://localhost:11222"
  #   cache_name: "horizon"
  #   username: ""
  #   password: ""

defaults:
  num_partitions: 3            # Default partitions for new topics
  replication_factor: 1        # Default replication factor

performance:
  write_buffer_kb: 2048        # Write buffer size
  max_connections: 10000       # Maximum concurrent connections
  io_threads: 4                # I/O worker threads
  read_buffer_size: 65536      # Read buffer size per connection
